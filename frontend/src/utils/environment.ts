import '../env';

// Utilities - Environment Variables
const {
  NEXT_PUBLIC_API_BASEURL,
  NEXT_PUBLIC_STORE_HOST,
  NEXT_PUBLIC_STORE_PORT,
} = process.env;

export {
  NEXT_PUBLIC_API_BASEURL,
  NEXT_PUBLIC_STORE_HOST,
  NEXT_PUBLIC_STORE_PORT,
};
